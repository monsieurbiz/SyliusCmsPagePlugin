{% import '@SyliusUi/Macro/buttons.html.twig' as buttons %}

{% set path = options.link.url|default(path(options.link.route|default(grid.requestConfiguration.getRouteName('preview')), options.link.parameters|default({'id': data.id}))) %}

{{ buttons.default(path, 'monsieurbiz_cms_page.ui.actions.preview', null, 'eye', 'blue') }}
